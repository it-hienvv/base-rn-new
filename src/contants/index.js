import * as screenNames from './screenNames';
import * as newWorks from './networkStatus';
import * as Strings from './String';
import * as Err from './defaultError';

export default CONTANTS = {
    screenNames,
    newWorks,
    Strings,
    Err
}